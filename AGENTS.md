# AGENTS.md

本文件约束本仓库中的自动化开发代理和协作者。它适用于仓库根目录及全部子目录。

## 1. 当前阶段

当前为**阶段 1：工程骨架与技术验证**。阶段 0 的产品、架构、流程和协作文档已经获得用户确认，并已推送到远程仓库。

当前只允许推进 `docs/DEVELOPMENT_PLAN.md` 中的阶段 1（S1.1-S1.6）：工程工具链、双进程空壳、IPC、Qt Widgets 壳、托盘生命周期、fake upstream 和流式技术验证。禁止跨阶段批量实现。

发现设计冲突时先更新文档或 ADR 并列出影响，再继续编码；阶段门禁未通过时不进入下一阶段。

## 2. 开始工作前必读

按顺序阅读：

1. `README.md`
2. `docs/PRODUCT_SPEC.md`
3. `docs/ARCHITECTURE.md`
4. `docs/DEVELOPMENT_PLAN.md`
5. 本文件

若代码行为与文档冲突，先确认冲突属于规格变化还是代码缺陷。规格变化要同步更新文档和验收标准。

## 3. 固定架构边界

- `modelharbor-desktop` 只负责 UI、托盘和网关进程管理。
- `modelharbor-gateway` 是数据库唯一写入者，也是秘密解密、路由和模型流量的所有者。
- Desktop 通过本地 IPC 调用 application 用例，不直接访问 SQLite。
- domain 不依赖 Qt Widgets、SQL、HTTP、DPAPI 或具体 Provider SDK。
- UI 不直接拼接 SQL、上游 HTTP 请求或 IPC JSON。
- Provider 差异必须落在 adapter 中，禁止在路由器散落 `if (provider == ...)`。
- 统计写入失败不得改变已成功返回给客户端的响应。

改变这些边界前，必须先写 ADR 或更新架构文档并获得审阅。

## 4. 工程基线

- C++20，禁止依赖编译器私有扩展完成核心逻辑。
- Qt 6.11.1；发布主工具链为 MSVC x64，CMake + Ninja。
- 第三方依赖通过 vcpkg manifest 和固定 baseline 管理。
- 使用 RAII 管理连接、并发槽、事务、curl handle 和敏感缓冲区。
- 所有权默认使用值、`std::unique_ptr` 或清晰的引用；共享所有权要说明原因。
- 时间间隔使用 `std::chrono::steady_clock`；持久化时间使用 UTC。
- 金额使用整数微美元或定点类型，禁止用 `double` 累计账单。
- 错误使用结构化错误码和上下文，不把用户可见文案当程序判断条件。
- 代码标识符和提交信息使用英文；用户界面与产品文档以简体中文为主。
- 中文文本统一 UTF-8；读取中文文件时显式确认编码。

## 5. 安全与隐私不变量

- Authorization、Cookie、API Key、access token、refresh token、代理密码和完整凭据永远不得写入日志。
- 保存前用 DPAPI CurrentUser 加密敏感字段；数据库只存密文和必要元数据。
- 本地 API Key 使用高熵随机生成，持久化带盐摘要。
- 客户 API 默认只绑定 `127.0.0.1`/`::1`；扩大监听范围属于产品规格变更。
- 默认不保存 prompt、请求正文和模型回答。
- 上游重定向默认关闭；TLS 证书与主机名校验默认开启。
- 导入先预览再提交；预览界面只显示掩码、尾号和不可逆指纹。
- 导出或诊断包必须经过统一脱敏器，并有包含敏感数据的自动化扫描测试。
- 新增日志字段时先写脱敏测试，再在生产日志中启用。

## 6. 路由不变量

- 先过滤候选，再选择仍有候选的最高优先级组。
- 同优先级启用严格轮询时，游标键为 route + logical model，所有线程共享同一调度状态。
- 游标只在成功取得渠道并发 lease 后推进。
- 同一请求维护已尝试渠道集合，禁止重试循环。
- 所有重试共享一个总截止时间。
- 流式响应首字已经下发后，禁止跨渠道重试和内容拼接。
- 每条退出路径都必须通过 RAII 释放并发 lease。
- 会话粘性默认关闭；启用时必须在 UI 和决策轨迹中可见。
- 路由器必须返回结构化候选过滤原因和最终选择原因。

任何路由修改至少运行：严格轮询公平性、SWRR 分布、并发竞争、熔断、冷却、跨优先级回退和流式重试边界测试。

## 7. 指标与账单不变量

- TTFT 是请求发出到首个有效模型内容事件的时长，不是收到响应头的时长。
- TTFB、TTFT 和 total 分开记录，字段缺失时保持未知。
- Token 未由上游返回时不得用字符数填入精确 Token 字段。
- 普通输入、缓存读取、缓存写入、输出、推理和音频等 Token 桶分开计价。
- 每条费用记录固定价格表版本、币种与汇率版本。
- A/B/C/U 证据等级必须随成本结果持久化和展示。
- 余额差分试验要独占目标凭据，普通流量不得进入试验窗口。

## 8. Sub2API 导入规则

- 首先支持 `sub2api-data` version 1、旧 `sub2api-bundle` 和 Codex JSON/JSONL。
- 解析、规范化、去重、预览、事务提交和后台验证必须是分离步骤。
- 原始 `credentials` 只在导入流水线的受控内存中存在，提交时转为 secret 引用。
- 保留未知非敏感扩展字段，便于兼容新版本；未知敏感字段按敏感数据处理。
- 导入成功不等于可调度。只有 adapter 存在且验证通过的账号进入候选池。
- 每种新账号类型都要覆盖刷新、过期、模型映射、usage、429、并发与脱敏测试。
- 测试夹具使用虚构凭据，禁止提交真实账号、Token、Cookie、邮箱和代理密码。

## 9. UI 规则

- 使用 Model/View，禁止为万级表格创建逐行 QWidget。
- GUI 线程不执行网络、迁移、批量导入、大 JSON 解析或价格重算。
- 图标采用 Lucide；按钮图标有工具提示，状态同时使用图标/文字而非只靠颜色。
- 卡片圆角不超过 8 px，不做卡片嵌套、装饰光斑或大面积渐变。
- 紧凑页面使用与容器匹配的字号，禁止在设置面板中使用英雄级标题。
- 固定格式控件要有稳定尺寸，长文本必须换行、省略或提供详情，不得遮挡相邻内容。
- 每个页面实现 loading、empty、error、partial、disabled 和 cancelling 状态。
- UI 变更至少检查 1100×700、1280×800、1920×1080，以及 100%/150%/200% 缩放。

## 10. 测试纪律

- 修改行为前先添加失败测试；修复缺陷时添加最小回归用例。
- 单元测试不得访问公网或真实站点。
- 网络测试统一使用仓库内 fake upstream，可编程返回慢响应、断流、429、401、5xx 和畸形数据。
- 时间相关测试注入虚拟时钟，禁止用长时间 `sleep` 等待状态变化。
- 调度测试注入确定性随机源。
- 数据库测试使用临时目录，并验证迁移、事务回滚和明文秘密扫描。
- 性能测试与正确性测试分开；性能阈值只在固定 Release 环境中判定。
- 测试失败时修复根因，禁止通过放宽核心公平性、金额或安全断言来让测试变绿。

项目骨架建立后，统一命令预期为：

```powershell
cmake --preset windows-msvc-debug
cmake --build --preset windows-msvc-debug
ctest --preset windows-msvc-debug --output-on-failure
```

在命令真实存在前，不在完成报告中声称已运行。

## 11. 数据库与兼容性

- schema 变更必须新增迁移，禁止编辑已经发布的迁移文件。
- 迁移应尽量可重复执行，并在事务内完成；SQLite 不支持的操作要有重建表测试。
- IPC、数据库、导入格式和指纹测试包分别版本化。
- 配置更新采用“事务提交 -> 构建新不可变路由快照 -> 原子发布”顺序。
- 删除字段前至少保留一个兼容读取周期。

## 12. 依赖与参考实现

- 引入依赖前记录用途、版本、许可证、二进制体积和维护状态。
- 优先使用现有 Qt、Boost、curl、SQLite 和 yyjson 能力，避免功能重叠依赖。
- 参考 `ai-relay-manager`、New API 和 Sub2API 的工作流、公开接口和数据格式。
- 不复制 New API、Sub2API 或参考程序源码到本仓库；若确需复用代码，先完成许可证与发布方式审阅。

## 13. 变更流程

1. 确认当前开发阶段和对应验收项。
2. 检查工作树，保留用户已有改动。
3. 先添加或更新测试夹具。
4. 做范围最小的实现，不混入无关重构。
5. 运行受影响的测试，再运行阶段门禁测试。
6. 更新行为文档、兼容矩阵和必要 ADR。
7. 报告变更、验证结果、剩余风险和供用户审阅的入口。

## 14. 完成定义

一个功能只有同时满足以下条件才算完成：

- 产品规格中的行为和边界已实现。
- 正常、错误、取消、超时和并发路径有测试。
- 日志、数据库和导出文件通过敏感数据扫描。
- 指标口径、路由轨迹和用户可见错误可解释。
- UI 包含完整状态并通过目标分辨率/DPI 检查。
- 文档、迁移、夹具和许可证同步更新。
- 所有相关自动化测试通过，未执行的验证在报告中明确列出。
